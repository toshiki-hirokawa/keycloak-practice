{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Rebuild and Start Keycloak",
      "type": "shell",
      "command": "sh",
      "args": [
        "-c",
        "docker compose down && cd custom-provider && mvn clean package && cd .. && docker compose up --build"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
